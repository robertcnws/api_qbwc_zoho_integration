apiVersion: v1
data:
  gunicorn.conf.py: |-
    bind = '0.0.0.0:8000'
    workers = 3
    threads = 2
    timeout = 1024
    graceful_timeout = 1024
    keepalive = 5
    accesslog = '-'
    errorlog = '-'
    loglevel = 'info'
    wsgi_app = 'project_api.wsgi:application'
kind: ConfigMap
metadata:
  annotations:
    use-subpath: "true"
  labels:
    io.kompose.service: backend-app
  name: backend-app-cm1
